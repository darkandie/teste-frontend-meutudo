import { HeaderTitle, Layout, NavBar } from "../components";

const PeriodPage = () => {
  return (
    <Layout>
      <NavBar />
      <HeaderTitle name="Periodo" />
    </Layout>
  );
};

export default PeriodPage;
