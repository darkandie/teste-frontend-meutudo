import { ClientInfo, Content, Layout, NavBar } from "../components";

const Home = () => {
  return (
    <Layout>
      <NavBar />
      <ClientInfo />
      <Content />
    </Layout>
  );
};

export default Home;
