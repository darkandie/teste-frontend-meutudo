import { HeaderTitle, Layout, NavBar } from "../components";

const EndPage = () => {
  return (
    <Layout>
      <NavBar />
      <HeaderTitle name="Opções" />
    </Layout>
  );
};

export default EndPage;
